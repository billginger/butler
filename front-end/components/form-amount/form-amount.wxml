<view class="form-item">
  <view class="form-item-label">
    <text>{{label}}</text>
  </view>
  <view class="form-item-input">
    <input type="{{type}}" maxlength="{{maxlength}}" value="{{value}}" bindinput="onInput" />
  </view>
</view>
